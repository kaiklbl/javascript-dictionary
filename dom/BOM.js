// Browser object Modell

window.alert('du ratte')

// gibt höhe vom screen zurück
window.screen.height;

// gibt die breite vom screen zurück
window.screen.width;

// zeigt mir den pfad(URL) wo man ist 
window.location.href;

// gibt den aktuellen Port zurück unter dem die Seite läuft
window.location.port;

// unter welchen übertragungsprotokoll es läuft
window.location.protocol;

// um direkt benutzer auf andere Seite weiter zu schicken
window.location.href = 'http://www.google.com';

// geht den Browserverlauf eine Seite zurück
window.history.back()

// geht Browserverlauf eine Seite vorwärts
window.history.forward();

// kann man userinpu entgegen nehmen
window.prompt()